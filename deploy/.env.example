# Copy to /opt/money-tracker/.env on the VPS and fill in values.
APP_IMAGE=ghcr.io/OWNER/REPO:latest
CADDY_DOMAIN=money.example.ru
BASIC_AUTH_HASH=
# App auth (required, the app refuses to start without them).
# Generate the secret: openssl rand -hex 32
AUTH_PIN=
AUTH_SECRET=
