services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
      DATA_DIR: "/data"
    volumes:
      - money_data:/data
    restart: unless-stopped

volumes:
  money_data:
